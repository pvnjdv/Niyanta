@import url('https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=Space+Mono:ital,wght@0,400;0,700;1,400&family=DM+Sans:wght@300;400;500;600&display=swap');

/* ─────────────────────────────────────────────
   DARK THEME (default)
───────────────────────────────────────────── */
:root,
[data-theme="dark"] {
  --bg-base:         #0A0A0F;
  --bg-panel:        #0E0E16;
  --bg-tile:         #13131E;
  --bg-tile-hover:   #181826;
  --bg-tile-active:  #1C1C2E;
  --bg-input:        #101018;
  --bg-msg-out:      #141422;
  --bg-msg-in:       #0E0E16;
  --bg-dock:         rgba(10,10,15,0.96);
  --bg-overlay:      rgba(0,0,0,0.80);
  --bg-glass:        rgba(14,14,22,0.92);
  
  /* Glass Morphism */
  --glass-card:      rgba(19,19,30,0.85);
  --glass-panel:     rgba(14,14,22,0.75);
  --glass-bg:        rgba(10,10,15,0.45);
  --glass-overlay:   rgba(255,255,255,0.04);

  --accent:          #00E676;
  --accent-dim:      rgba(0,230,118,0.10);
  --accent-glow:     rgba(0,230,118,0.22);
  --accent-border:   rgba(0,230,118,0.36);
  --accent-text:     #00E676;

  --status-success:  #00E676;
  --status-warning:  #FFB300;
  --status-danger:   #FF3D71;
  --status-info:     #00BCD4;
  --status-neutral:  #3A3A4A;

  --text-primary:    #EAEAF2;
  --text-secondary:  #72728A;
  --text-muted:      #3A3A52;
  --text-green:      #00E676;

  --border:          rgba(255,255,255,0.08);
  --border-active:   rgba(0,230,118,0.55);
  --border-hover:    rgba(0,230,118,0.30);
  --border-subtle:   rgba(255,255,255,0.04);
  --border-danger:   rgba(255,61,113,0.45);
  --border-warning:  rgba(255,179,0,0.45);

  --shadow-tile:     0 1px 3px rgba(0,0,0,0.6), 0 0 0 1px rgba(255,255,255,0.05);
  --shadow-active:   0 0 0 1px var(--accent-border), 0 4px 24px var(--accent-glow);
  --shadow-panel:    0 8px 40px rgba(0,0,0,0.8);
  --shadow-dock:     0 -1px 0 rgba(255,255,255,0.06), 0 -8px 32px rgba(0,0,0,0.5);
  --shadow-modal:    0 24px 80px rgba(0,0,0,0.9), 0 0 0 1px rgba(255,255,255,0.07);
  --shadow-card:     0 2px 8px rgba(0,0,0,0.5);
  --shadow-elevated: 0 4px 24px rgba(0,0,0,0.7);

  /* Scrollbar */
  --scroll-thumb:    rgba(0,230,118,0.18);
  --scroll-hover:    rgba(0,230,118,0.32);
}

/* ─────────────────────────────────────────────
   LIGHT THEME — Materialistic white
───────────────────────────────────────────── */
[data-theme="light"] {
  --bg-base:         #F0F2F7;
  --bg-panel:        #FFFFFF;
  --bg-tile:         #FFFFFF;
  --bg-tile-hover:   #F5F7FF;
  --bg-tile-active:  #EEF2FF;
  --bg-input:        #F8F9FC;
  --bg-msg-out:      #F1F5F9;
  --bg-msg-in:       #FFFFFF;
  --bg-dock:         rgba(255,255,255,0.97);
  --bg-overlay:      rgba(30,30,60,0.45);
  --bg-glass:        rgba(255,255,255,0.95);
  
  /* Glass Morphism */
  --glass-card:      rgba(255,255,255,0.85);
  --glass-panel:     rgba(255,255,255,0.75);
  --glass-bg:        rgba(255,255,255,0.45);
  --glass-overlay:   rgba(0,0,0,0.04);

  --accent:          #0052CC;
  --accent-dim:      rgba(0,82,204,0.08);
  --accent-glow:     rgba(0,82,204,0.18);
  --accent-border:   rgba(0,82,204,0.35);
  --accent-text:     #0052CC;

  --status-success:  #00875A;
  --status-warning:  #E07B00;
  --status-danger:   #DE350B;
  --status-info:     #0065BD;
  --status-neutral:  #DFE1E6;

  --text-primary:    #172B4D;
  --text-secondary:  #5E6C84;
  --text-muted:      #97A0AF;
  --text-green:      #00875A;

  --border:          rgba(9,30,66,0.12);
  --border-active:   rgba(0,82,204,0.55);
  --border-hover:    rgba(0,82,204,0.32);
  --border-subtle:   rgba(9,30,66,0.06);
  --border-danger:   rgba(222,53,11,0.35);
  --border-warning:  rgba(224,123,0,0.40);

  --shadow-tile:     0 1px 2px rgba(9,30,66,0.08), 0 0 0 1px rgba(9,30,66,0.08);
  --shadow-active:   0 0 0 2px var(--accent-border), 0 4px 20px var(--accent-glow);
  --shadow-panel:    0 8px 32px rgba(9,30,66,0.14);
  --shadow-dock:     0 -1px 0 rgba(9,30,66,0.10), 0 -4px 24px rgba(9,30,66,0.08);
  --shadow-modal:    0 20px 60px rgba(9,30,66,0.22), 0 0 0 1px rgba(9,30,66,0.10);
  --shadow-card:     0 2px 8px rgba(9,30,66,0.12), 0 0 0 1px rgba(9,30,66,0.06);
  --shadow-elevated: 0 4px 20px rgba(9,30,66,0.16);

  --scroll-thumb:    rgba(0,82,204,0.20);
  --scroll-hover:    rgba(0,82,204,0.38);
}

/* ─────────────────────────────────────────────
   SHARED FONTS
───────────────────────────────────────────── */
:root {
  --font-display: 'Syne', sans-serif;
  --font-mono:    'Space Mono', monospace;
  --font-body:    'DM Sans', sans-serif;
}

/* ─────────────────────────────────────────────
   RESET
───────────────────────────────────────────── */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  text-rendering: optimizeLegibility;
}

body {
  background: var(--bg-base);
  color: var(--text-primary);
  font-family: var(--font-body);
  font-size: 13px;
  font-weight: 400;
  line-height: 1.5;
  height: 100vh;
  overflow: hidden;
  transition: background 250ms ease, color 200ms ease;
  position: relative;
}

body::before {
  content: '';
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: 400px;
  background: radial-gradient(ellipse at top, var(--accent-glow), transparent 70%);
  opacity: 0.15;
  pointer-events: none;
  z-index: 0;
}

#root {
  height: 100vh;
  overflow: hidden;
  display: flex;
  flexDirection: column;
  position: relative;
  z-index: 1;
}

/* ─────────────────────────────────────────────
   SCROLLBAR
───────────────────────────────────────────── */
::-webkit-scrollbar        { width: 5px; height: 5px; }
::-webkit-scrollbar-track  { background: transparent; }
::-webkit-scrollbar-thumb  { background: var(--scroll-thumb); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: var(--scroll-hover); }

/* ─────────────────────────────────────────────
   SELECTION & FOCUS
───────────────────────────────────────────── */
::selection { background: var(--accent-dim); color: var(--text-primary); }

:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 1px;
}

/* ─────────────────────────────────────────────
   LINKS & BUTTONS
───────────────────────────────────────────── */
a { color: var(--accent-text); text-decoration: none; }
a:hover { text-decoration: underline; }

button {
  cursor: pointer;
  font-family: inherit;
  font-size: inherit;
  border: none;
  background: transparent;
  color: inherit;
  transition: background 120ms ease, border-color 120ms ease, color 120ms ease, box-shadow 120ms ease;
}
button:disabled { cursor: not-allowed; opacity: 0.38; }
button:not(:disabled):active { transform: scale(0.98); }

/* ─────────────────────────────────────────────
   INPUTS
───────────────────────────────────────────── */
input, textarea, select {
  font-family: var(--font-body);
  font-size: 13px;
  color: var(--text-primary);
  background: var(--bg-input);
  border: 1px solid var(--border);
  border-radius: 3px;
  padding: 8px 12px;
  outline: none;
  transition: border-color 150ms ease, box-shadow 150ms ease, background 150ms ease;
}
input:focus, textarea:focus, select:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 2px var(--accent-glow);
}

[data-theme="light"] input,
[data-theme="light"] textarea,
[data-theme="light"] select {
  background: #FFFFFF;
  border-color: rgba(9,30,66,0.15);
}
[data-theme="light"] input:focus,
[data-theme="light"] textarea:focus,
[data-theme="light"] select:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 2px rgba(0,82,204,0.14);
}

/* ─────────────────────────────────────────────
   MATERIAL TILES — the core card system
───────────────────────────────────────────── */
.tile {
  background: var(--bg-tile);
  border: 1px solid var(--border);
  border-radius: 4px;
  box-shadow: var(--shadow-card);
  transition: box-shadow 150ms ease, border-color 150ms ease, transform 100ms ease;
}
.tile:hover {
  box-shadow: var(--shadow-elevated);
  border-color: var(--border-hover);
}
[data-theme="light"] .tile {
  box-shadow: 0 2px 8px rgba(9,30,66,0.10), 0 0 0 1px rgba(9,30,66,0.06);
}
[data-theme="light"] .tile:hover {
  box-shadow: 0 4px 16px rgba(9,30,66,0.14), 0 0 0 1px rgba(0,82,204,0.25);
}

/* ─────────────────────────────────────────────
   SECTION HEADERS
───────────────────────────────────────────── */
.section-header {
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 16px;
  border-bottom: 1px solid var(--border);
}
.section-label {
  font-family: var(--font-mono);
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--text-secondary);
}
[data-theme="light"] .section-label {
  color: var(--text-secondary);
}

/* ─────────────────────────────────────────────
   STATUS BADGES
───────────────────────────────────────────── */
.badge {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  height: 20px;
  padding: 0 8px;
  font-family: var(--font-mono);
  font-size: 9px;
  font-weight: 700;
  letter-spacing: 0.10em;
  text-transform: uppercase;
  border-radius: 2px;
  border: 1px solid;
}
.badge--success { background: rgba(0,135,90,0.10); border-color: rgba(0,135,90,0.30); color: var(--status-success); }
.badge--warning { background: rgba(224,123,0,0.10); border-color: rgba(224,123,0,0.30); color: var(--status-warning); }
.badge--danger  { background: rgba(222,53,11,0.10); border-color: rgba(222,53,11,0.30); color: var(--status-danger); }
.badge--info    { background: rgba(0,101,189,0.10); border-color: rgba(0,101,189,0.30); color: var(--status-info); }
.badge--neutral { background: var(--border-subtle); border-color: var(--border); color: var(--text-secondary); }

/* ─────────────────────────────────────────────
   STAT TILES
───────────────────────────────────────────── */
.stat-tile {
  padding: 20px;
  background: var(--bg-tile);
  border: 1px solid var(--border);
  border-radius: 4px;
  box-shadow: var(--shadow-card);
  transition: box-shadow 150ms, transform 100ms;
}
.stat-tile:hover {
  box-shadow: var(--shadow-elevated);
  transform: translateY(-1px);
}
[data-theme="light"] .stat-tile {
  box-shadow: 0 2px 6px rgba(9,30,66,0.08), 0 0 0 1px rgba(9,30,66,0.06);
}
[data-theme="light"] .stat-tile:hover {
  box-shadow: 0 6px 20px rgba(9,30,66,0.14);
}

/* ─────────────────────────────────────────────
   TABLE
───────────────────────────────────────────── */
.nyt-table {
  width: 100%;
  border-collapse: collapse;
}
.nyt-table th {
  height: 36px;
  padding: 0 12px;
  text-align: left;
  font-family: var(--font-mono);
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.10em;
  color: var(--text-secondary);
  border-bottom: 1px solid var(--border);
  background: var(--bg-tile-hover);
}
.nyt-table td {
  height: 48px;
  padding: 0 12px;
  font-family: var(--font-body);
  font-size: 13px;
  border-bottom: 1px solid var(--border-subtle);
  vertical-align: middle;
}
.nyt-table tr:hover td {
  background: var(--bg-tile-hover);
}
[data-theme="light"] .nyt-table th {
  background: #F8F9FC;
  border-bottom-color: rgba(9,30,66,0.12);
}
[data-theme="light"] .nyt-table td {
  border-bottom-color: rgba(9,30,66,0.06);
}

/* ─────────────────────────────────────────────
   PANEL BORDERS / DIVIDERS
───────────────────────────────────────────── */
.panel-divider {
  width: 1px;
  background: var(--border);
  align-self: stretch;
  flex-shrink: 0;
}
.row-divider {
  height: 1px;
  background: var(--border);
  width: 100%;
}
[data-theme="light"] .panel-divider,
[data-theme="light"] .row-divider {
  background: rgba(9,30,66,0.10);
}

/* ─────────────────────────────────────────────
   TOP BAR
───────────────────────────────────────────── */
.topbar {
  background: var(--bg-panel);
  border-bottom: 1px solid var(--border);
  box-shadow: 0 1px 4px var(--accent-glow);
}
[data-theme="light"] .topbar {
  background: #FFFFFF;
  border-bottom-color: rgba(9,30,66,0.12);
  box-shadow: 0 1px 4px rgba(9,30,66,0.08);
}

/* ─────────────────────────────────────────────
   BOTTOM DOCK
───────────────────────────────────────────── */
.dock-container {
  background: var(--bg-dock);
  border: 1px solid var(--border);
  box-shadow: var(--shadow-dock);
  backdrop-filter: blur(24px) saturate(180%);
  border-radius: 8px;
}
[data-theme="light"] .dock-container {
  background: rgba(255,255,255,0.97);
  border-color: rgba(9,30,66,0.12);
  box-shadow: 0 -2px 0 rgba(9,30,66,0.08), 0 4px 32px rgba(9,30,66,0.12);
}

/* ─────────────────────────────────────────────
   THEME TOGGLE BUTTON
───────────────────────────────────────────── */
.theme-toggle {
  width: 36px;
  height: 20px;
  background: var(--bg-tile-hover);
  border: 1px solid var(--border);
  border-radius: 10px;
  position: relative;
  transition: background 200ms, border-color 200ms;
  cursor: pointer;
  flex-shrink: 0;
}
.theme-toggle__knob {
  position: absolute;
  top: 2px;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  transition: left 200ms ease, background 200ms;
}
[data-theme="dark"] .theme-toggle         { background: rgba(0,230,118,0.12); border-color: rgba(0,230,118,0.30); }
[data-theme="dark"] .theme-toggle__knob   { left: 2px; background: #00E676; }
[data-theme="light"] .theme-toggle        { background: rgba(0,82,204,0.08); border-color: rgba(0,82,204,0.28); }
[data-theme="light"] .theme-toggle__knob  { left: 18px; background: #0052CC; }

/* ─────────────────────────────────────────────
   ANIMATIONS
───────────────────────────────────────────── */
@keyframes accentPulse {
  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 var(--accent-glow); }
  50%       { opacity: 0.7; box-shadow: 0 0 10px 3px var(--accent-glow); }
}
@keyframes blink {
  0%, 80%, 100% { transform: scale(0.5); opacity: 0.3; }
  40%           { transform: scale(1); opacity: 1; }
}
@keyframes slideInBottom {
  from { opacity: 0; transform: translateY(14px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes slideInRight {
  from { opacity: 0; transform: translateX(20px); }
  to   { opacity: 1; transform: translateX(0); }
}
@keyframes slideInTop {
  from { opacity: 0; transform: translateY(-10px); }
  to   { opacity: 1; transform: translateY(0); }
}
@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}
@keyframes fadeScale {
  from { opacity: 0; transform: scale(0.97); }
  to   { opacity: 1; transform: scale(1); }
}
@keyframes criticalFlash {
  0%, 100% { background: rgba(222,53,11,0.08); border-color: rgba(222,53,11,0.40); }
  50%       { background: rgba(222,53,11,0.18); border-color: rgba(222,53,11,0.80); }
}
@keyframes spin  { to { transform: rotate(360deg); } }
@keyframes dockBounce {
  0%, 100% { transform: translateY(0) scale(1); }
  50%       { transform: translateY(-5px) scale(1.08); }
}
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50%       { opacity: 0.4; }
}
@keyframes shimmer {
  from { background-position: -200% 0; }
  to   { background-position: 200% 0; }
}
@keyframes glowPulse {
  0%, 100% { box-shadow: 0 0 20px var(--accent-glow); }
  50%       { box-shadow: 0 0 40px var(--accent-glow), 0 0 60px var(--accent-glow); }
}
@keyframes floatUp {
  from { opacity: 0; transform: translateY(20px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* Legacy compat aliases */
@keyframes greenPulse {
  0%, 100% { opacity: 1; box-shadow: 0 0 0 0 var(--accent-glow); }
  50%       { opacity: 0.7; box-shadow: 0 0 8px 2px var(--accent-glow); }
}

/* ─────────────────────────────────────────────
   LEGACY VAR ALIASES — keep old names working
───────────────────────────────────────────── */
:root,
[data-theme="dark"],
[data-theme="light"] {
  --green-primary:  var(--accent);
  --green-dim:      var(--accent-dim);
  --green-glow:     var(--accent-glow);
  --green-border:   var(--accent-border);
  --text-green:     var(--accent-text);

  --shadow-active: var(--shadow-active, 0 0 0 1px var(--accent-border));
}

/* ─────────────────────────────────────────────
   THEME TRANSITION
───────────────────────────────────────────── */
*, *::before, *::after {
  transition-property: background-color, border-color, color, box-shadow;
  transition-duration: 200ms;
  transition-timing-function: ease;
}
/* Disable slow transitions on transforms/animations */
button, [class*="animate"], [style*="animation"] {
  transition-property: background-color, border-color, color, box-shadow, opacity, transform;
}

/* ─────────────────────────────────────────────
   GLASS MORPHISM UTILITIES
───────────────────────────────────────────── */
.glass-card {
  background: var(--glass-card);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--border);
  border-radius: 24px;
  overflow: hidden;
  position: relative;
  box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.4), 0 10px 10px -5px rgba(0, 0, 0, 0.2);
}

.glass-card:hover {
  border-color: var(--border-hover);
}

.glass-card::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, var(--glass-overlay) 0%, transparent 50%, transparent 100%);
  opacity: 0;
  transition: opacity 300ms ease;
  pointer-events: none;
  z-index: 0;
}

.glass-card:hover::before {
  opacity: 1;
}

.glass-panel {
  background: var(--glass-panel);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid var(--border);
  border-radius: 16px;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
}

.glass-bg {
  background: var(--glass-bg);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}

.glass-button {
  background: var(--glass-bg);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 8px 16px;
  transition: all 300ms cubic-bezier(0.25, 1.1, 0.4, 1);
}

.glass-button:hover {
  background: var(--bg-tile-hover);
  border-color: var(--border-hover);
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
}

.glass-input {
  background: var(--glass-bg);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid var(--border);
  border-radius: 12px;
  padding: 12px 16px;
  outline: none;
  transition: all 200ms ease;
}

.glass-input:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-glow);
  background: var(--bg-input);
}

/* Rounded corners for modern look */
.rounded-xl { border-radius: 12px; }
.rounded-2xl { border-radius: 16px; }
.rounded-3xl { border-radius: 24px; }
.rounded-full { border-radius: 9999px; }

/* Modern gradients */
.gradient-overlay {
  background: linear-gradient(135deg, var(--glass-overlay), transparent, var(--glass-overlay));
}

/* ─────────────────────────────────────────────
   ENHANCED GLASS EFFECTS & UTILITIES
───────────────────────────────────────────── */

/* Frosted glass effect */
.frosted-glass {
  background: var(--glass-bg);
  backdrop-filter: blur(24px) saturate(180%);
  -webkit-backdrop-filter: blur(24px) saturate(180%);
  border: 1px solid var(--border);
}

/* Glass shimmer effect */
.glass-shimmer {
  position: relative;
  overflow: hidden;
}

.glass-shimmer::after {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    90deg,
    transparent,
    rgba(255, 255, 255, 0.05),
    transparent
  );
  animation: shimmer 3s infinite;
}

/* Glow effects */
.glow-accent {
  box-shadow: 0 0 20px var(--accent-glow), 0 0 40px var(--accent-glow);
}

.glow-accent-hover:hover {
  box-shadow: 0 0 30px var(--accent-glow), 0 0 60px var(--accent-glow);
  transition: box-shadow 300ms ease;
}

/* Card stack effect */
.card-stack {
  position: relative;
}

.card-stack::before,
.card-stack::after {
  content: '';
  position: absolute;
  left: 4px;
  right: 4px;
  height: 100%;
  border-radius: inherit;
  border: 1px solid var(--border);
  background: var(--glass-bg);
  z-index: -1;
}

.card-stack::before {
  top: 4px;
  opacity: 0.5;
}

.card-stack::after {
  top: 8px;
  opacity: 0.25;
}

/* Neon border effect */
.neon-border {
  border: 1px solid var(--accent);
  box-shadow: 
    0 0 5px var(--accent),
    inset 0 0 5px var(--accent-glow);
}

/* Smooth hover lift */
.hover-lift {
  transition: transform 300ms cubic-bezier(0.25, 1.1, 0.4, 1);
}

.hover-lift:hover {
  transform: translateY(-4px);
}

/* Text glow */
.text-glow {
  text-shadow: 0 0 10px var(--accent-glow), 0 0 20px var(--accent-glow);
}

/* Float animation */
.float {
  animation: floatUp 0.6s cubic-bezier(0.25, 1, 0.5, 1) forwards;
}

/* Status indicators with glass */
.status-indicator {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 12px;
  background: var(--glass-bg);
  backdrop-filter: blur(8px);
  border: 1px solid var(--border);
  border-radius: 12px;
  font-family: var(--font-mono);
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

/* Modern scrollbar for glass elements */
.glass-scroll::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

.glass-scroll::-webkit-scrollbar-track {
  background: var(--glass-bg);
  border-radius: 4px;
}

.glass-scroll::-webkit-scrollbar-thumb {
  background: var(--accent-border);
  border-radius: 4px;
  transition: background 200ms;
}

.glass-scroll::-webkit-scrollbar-thumb:hover {
  background: var(--accent);
}

/* Overlay backdrop */
.overlay-backdrop {
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  background: var(--bg-overlay);
}

/* Enhanced focus states */
.focus-ring:focus-visible {
  outline: 2px solid var(--accent);
  outline-offset: 2px;
  box-shadow: 0 0 0 4px var(--accent-glow);
}

/* Gradient text */
.gradient-text {
  background: linear-gradient(135deg, var(--accent), var(--text-primary));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
